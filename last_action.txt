add base app to settings.py/installed_apps
make homepage.html in base app, that will be used as a base page for each app
include urls (base, energy meter)
include function to energy_meter_base.html -woooooow

create view and form for template energy_meter_record_create.html (createview)

put the form of new record and list of record to one view - createview

create function for counting consumption, days of measuring etc.

app data, form, link on homepage

prepare data for chart

!!! update requirements.txt, continue with chart
